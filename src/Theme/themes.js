import {createTheme} from "@mui/material"

const themeOne = createTheme({
    palette: {
        white: {
          main: "#ffffff",
        },
        black: {
            main: "#000000",
          },
      },
})

export  {themeOne}